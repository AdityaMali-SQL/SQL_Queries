1.
select * from salespeople;

2.
select cname,rating from customers where rating=100;

3.	
select * from customers where city is null;

4.
select max(amt) , odate , s.snum from orders o join salespeople s on o.snum = s.snum group by s.snum , odate order by s.snum , odate;

5.
select * from orders order by cnum desc;

6. select distinct * from salespeople as s join orders as o on s.snum=o.snum;

7. 
select c.cnum,c.cname,s.snum,s.sname from customers as c join salespeople as s on c.snum = s.snum;

8.
select s.sname , s.snum , count(c.cnum) as total_Customers from customers as c join salespeople as s on c.snum=s.snum group by s.sname, s.snum having count(*) > 1;

9.
select s.snum , s.sname , count(o.onum) as Total_Orders from orders as o join salespeople as s on o.snum = s.snum group by s.sname,s.snum order by count(*) desc;

10. select * from customers;

11.
select s.sname , c.cname , s.city from salespeople as s join customers as c on s.snum=c.snum where s.city=c.city;

12. 
select snum , max(amt) from orders group by snum;

13. 
select cnum , cname , city from customers where rating >= 200 AND city='San Jose';

14.
select sname , comm from salespeople where city = 'London';

15.
select * from orders as o join salespeople as s on o.snum=s.snum where s.sname='Motika';

16.
select c.cnum, c.cname , o.odate from customers AS c JOIN orders as o ON c.cnum = o.cnum where o.odate = '1996-10-03';

17. 
select sum(amt) , odate from orders group by odate having sum(amt) >= (Max(amt) + 2000);

18. 
select * from orders where amt > ANY ( select amt from orders where odate='1996-10-06');

19.
select * from salespeople as s where exists ( select 1 from customers as c where rating=300 AND s.snum=c.snum);

20. 
select c1.cname , c2.cname from customers as c1 join customers as c2 on c1.rating=c2.rating and c1.cnum < c2.cnum;

21.
select c.cname , c.snum from customers as c join salespeople as s on s.snum=c.snum where s.sname = 'serres' and c.cnum = (1000+s.snum) ;

22.
select snum , sname , concat( comm*100 , '%')  as CommisionPercentage from salespeople ;

23.
select s.sname , s.snum , o.odate , max(o.amt) from orders as o join salespeople as s on o.snum=s.snum group by s.sname , s.snum , o.odate having max(o.amt)>=3000;

24.
select s.snum , s.sname , max(o.amt) from salespeople as s join orders as o on s.snum=o.snum where o.odate='1996-10-03' group by s.snum , s.sname order by max(amt) desc;

25.
select cnum , cname , city  from customers where city in ( select c.city from customers as c join salespeople as s on c.snum=s.snum where s.sname = 'serres');


26.
select * from customers where rating > 200;

27.
select count(*) as Number_of_Salespeople from orders as o join salespeople as s where o.snum=s.snum;

28.

select c.cname , (s.comm*100) as Commision_Percentage from customers as c join salespeople as s on c.snum=s.snum where (s.comm*100)>12;

29.
select s.snum , s.sname from salespeople as s join customers as c on s.snum = c.snum group by s.snum , s.sname having count(c.snum)>1 ;

30.
select sname , city from salespeople as s where exists ( select 1 from customers as c where s.snum = c.snum and s.city = c.city)

